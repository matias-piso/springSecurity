package com.example.demo.security.token;
// Esta es una enumeración que se utiliza para definir los diferentes tipos de tokens de autenticación que se pueden utilizar en la aplicación.

public enum TokenType {
    // El único valor en esta enumeración se llama "BEARER"
    // y es utilizado por el protocolo de autenticación OAuth 2.0 para indicar que el token es un token de portador (bearer token).
    BEARER
}